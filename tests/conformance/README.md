# K8 Conformance Vectors

Each vector specifies initial CPU and memory state, instruction bytes, execution count, and expected final CPU/memory state.

The vectors are the contract between the architecture specification and implementations.